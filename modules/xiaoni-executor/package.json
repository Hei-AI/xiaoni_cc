{
  "name": "qq-bot-xiaoni-executor",
  "version": "1.0.0",
  "description": "Durable command executor runtime for Xiaoni",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "npm run clean && npm run build && node --test dist/__tests__/*.test.js",
    "lint": "echo 'No xiaoni-executor lint step configured'",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.5.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}
