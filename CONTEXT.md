# CONTEXT

小腻 runtime 的术语表。只解释**词**，不解释实现——实现看 `docs/INDEX.md` 指向的专项文档。

## 主 agent

小腻本人。唯一有工具、能碰世界的那个 agent。她的上下文是一条追加式的栈，跨 run 连续。
**规划是她的职责**：未来要做什么，由她决定、由她执行。

## 潜意识 fork

主 agent 每次「停下来」（settle）之后跑的一次性克隆请求。它复用主 agent 的完整上下文，
但**不碰世界**——唯一的出口是交出一份 [xiaoni_plan](#xiaoni_plan)。

它是**唤醒器，不是规划器**。它的职责是让停下来的主 agent 重新动起来，而不是替她想好该做什么。
这条边界是有意的：潜意识看得到的东西和主 agent 一样，但它没有手，替她规划只会产出她不执行的东西。

## xiaoni_plan

潜意识 fork 交给主 agent 的那份东西。两种形状，由 fork 自己看着上下文里的历史 plan 决定用哪种：

- **指认**——过去的 plan 大部分没做时用。不复述旧内容，只点名其中最要紧的一件，要求做完。
- **规划**——过去的 plan 做成了不少时用。给 5-6 个新方向。

「方向」不是「步骤」：方向是够钻一阵的中长期目标，步骤是两下就做完的小事。plan 只给方向。

## 自驱动 run

由 xiaoni_plan 唤醒的主 agent run，区别于由真实外部消息（QQ 消息等）唤醒的 run。
两者走同一条主 loop，区别只在唤醒它的是谁。

## 空转

主 agent 被唤醒之后没有真的做事——只说话、跑空操作命令、或者直接去休息。
空转的治理**不归潜意识 fork 管**：fork 只负责喊一嗓子，喊了不动是另一条腿的事。

## 唤醒 loop

「主 agent 停下来 → 潜意识 fork 唤醒 → 主 agent 又停下来 → 再唤醒」这个自我驱动的循环。
它是小腻在没有外部消息时仍然继续活动的机制。
