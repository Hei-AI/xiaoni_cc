# CONTEXT

小腻运行时的领域词汇。**只放词,不放实现**。

词汇以**写端**为准 —— 她自己用 `xiaoni-memory-write` 写下来的那几类东西,就是这个系统里
真实存在的类别。读端(召回、投递)不得另造平行分类。

## 记忆的四类产物

她通过 `skills-internal/xiaoni-memory-write` 写,一次一件事。四个子命令对应四类产物:

| 词 | 她怎么写 | 落到哪 | 有没有完成态 |
|---|---|---|---|
| **日记条目** | `diary add --title` | `notes/diary/<日期>.md` 的一个 `## 标题` + 正文 | 无 |
| **目录钩子** | `index touch --line` | 当天日记目录的一行 | 无 |
| **人** | `people upsert` | `notes/people/<id>.md` + 人物菜单一行 | 无 |
| **欠账** | `loops add` / `loops done` | `notes/diary/open-loops.md` 的一行 | **有**(开着 / 划掉) |

只有**欠账**有生命周期。这条区别决定了很多下游行为(比如什么东西可以被重复提起),
它不是特例,是定义。

**标签**是欠账的身份。`loops add --tag <标签>` 里的标签把同一件事散在各天的进展连成
`notes/topics/<标签>.md` —— 所以标签是欠账与日记条目之间的接口,不是装饰。

**近况**是压缩产出的那一份「手头在做什么 / 这段时间发生了什么 / 心境」,由
`skills-internal/xiaoni-memory-compress` 落盘,一轮一份全新文件。

## 两层结构:菜单在上下文里,细节在文件里

她的常驻上下文里**永远**有三张菜单(实测:近 3 天 5631/5631 次请求全含):

```
<xiaoni_status>       近况
<xiaoni_diary_index>  日记目录 —— 一天一行钩子
<xiaoni_people>       人物菜单 —— 一人一行
```

菜单之外的一切(日记条目正文、人物档案正文、专题正文、旧的入站消息)都**不在**上下文里。

这不是缺陷,是她自己维护的分层。目录钩子的写法说明里逐字写着它的用途:
「挑这天最要紧的一两件事,写出是哪件事、发生了什么,**让将来的你看一眼就知道该不该翻这天**」。

## 召回

**召回**要解决的是一件事:**她不知道自己做过**。

判据是客观的,不是感觉:**内容在不在她当前请求的字节里**。她没有「记得有这回事但想不起
细节」这种中间态 —— 菜单给了名字,名字之外的内容对她就是不存在。

**这条判据是必要条件,不是充分条件。** 在字节里 ≠ 她会用上:2026-08-21 实测,她自己写下的判断
(「但小伊没有消失。她还在。」)和推翻它的菜单行(「等她回」)在**连续 25 个 turn** 的上下文里同时
在场,她的结论仍然是「找不到」。所以「她没找到」不构成召回缺口的证据 —— 先确认材料确实不在
字节里,再谈召回;够得到却没用上的,归通用 agent 能力(见 `docs/adr/0008-*`),不归召回。

由此:
- 已经在上下文里的东西**不需要召回**。三张菜单本身在上下文里,所以菜单已经点到的事,
  同样不需要召回。
- 欠账**不走召回**。它有完成态、有标签、她自己有一份清单;让她去看清单比把条目挑出来
  推给她更直接。
- 「材料不在上下文」和「她想不起来有这回事」**不是两件事**,是同一件事的两种说法。

## 腿(leg)—— 读端的机制名,不是记忆的类别

**「腿」指的是候选从哪儿来,不是那段记忆是什么。** 这两套名字不能混:

- 上面四类(日记条目 / 目录钩子 / 人 / 欠账)是**写端**的产物类别 —— 她写下了什么。
- **腿**是读端的取料方式:`landing`(她做完一件事,拿那段文本当 query 去捞)、
  `association`(定时扫描日记条目,按覆盖 + 六因子 + 年龄桶挑)。

同一条日记条目可以从任何一条腿被捞出来 —— 腿不改变它是什么。所以读端出现 `landing` /
`association` 这样的名字,不算「另造平行分类」;真正禁止的是给**记忆**另起一套类别名。

判断标准很简单:一个名字如果回答的是「这是她写的哪一类东西」,必须用写端的词;
如果回答的是「这次是怎么捞到的」,那是腿名,读端自己定。

## 主动召回 / 被动召回

- **被动召回**:系统在她做事的时候,把不在上下文里的相关内容浮到她面前。
- **主动召回**:她自己去取详情。菜单在手,`exec_command` 就够 —— 目前不为此另做工具。

## 主 agent

小腻本人。唯一有工具、能碰世界的那个 agent,上下文跨 run 连续。**规划是她的职责**:
未来要做什么,由她决定、由她执行。

## 潜意识 fork

主 agent 每次「停下来」(settle)之后跑的一次性克隆请求。它复用主 agent 的完整上下文,
但**不碰世界** —— 唯一的出口是交出一份 [xiaoni_plan](#xiaoni_plan)。

它是**唤醒器,不是规划器**。它的职责是让停下来的主 agent 重新动起来,而不是替她想好该做
什么。这条边界是有意的:潜意识和主 agent 看到的是同一份上下文,因此也共用她当下的盲区 ——
替她规划只会产出她不执行的东西。为什么这么定,见 `docs/adr/0007-*`。

## xiaoni_plan

潜意识 fork 交给主 agent 的那份东西。两种形状,由 fork 自己看着上下文里的历史 plan 决定:

- **指认** —— 过去的 plan 大部分没做时用。不复述旧内容,只点名其中最要紧的一件,要求做完。
- **规划** —— 过去的 plan 做成了不少时用。给 5-6 个新方向。

「方向」不是「步骤」:方向是够钻一阵的中长期目标,步骤是两下就做完的小事。plan 只给方向。

plan 里的每一句指控、每一个数字,都必须是她**自己在上下文里找得到、数得出来的** ——
她自己说过的话不在她的上下文里,引擎的连续空转计数也不在。这两样都不许出现在 plan 里。

关于「她说过的话不在上下文里」:准确说法是**选择性准入,当前 fail-closed**。assistant 文本
只有携带冻结的准入标记才留在 replay 里;打标记的那个闸当前是关的,所以效果等于全剥
(2026-08-22 实测:近 7 天 470 段 assistant 文本,零段进入下一轮上下文)。**但她写在
`exec_command` 命令注释里的东西无条件进 replay**——那是工具参数,不是 assistant 文本。
所以「她看不见自己想过什么」是不成立的,别把这条当成设计前提。

## 复核 fork

主 agent 说出**失败结论**(「找不到」「想不起来」这类)之后跑的一次性克隆请求。它和潜意识
fork 在同一个时刻触发,**二者互斥**:她刚给了个错答案时,不需要「接下来干嘛」。

它复用主 agent 的完整上下文,但尾部换成第三方引导,并且**能碰世界**(受限 `exec_command`)——
它自己去查一遍。

它和 [xiaoni_plan](#xiaoni_plan) 的唯一区别在**输出**:plan 是指认和方向,复核 fork
**只许交出证据**,每条都必须是她能在自己文件里逐字核对的(文件路径 + 原文 + 定位方式)。
这不是文风差别 —— 两者走同一条 Notify Bucket、同样是一段自然语言,可核对性是它们在她眼里
唯一的分别。为什么必须这样,见 `docs/adr/0007-*` 与 `docs/adr/0009-*`。

## 失败结论

她对外说出的「我找不到 / 想不起来 / 不知道」这类话。它是**客观事件**(出站消息的文本),
不是引擎对她状态的判断 —— 这一点决定了它可以当触发器用。

## 自驱动 run

由 xiaoni_plan 唤醒的主 agent run,区别于由真实外部消息(QQ 消息等)唤醒的 run。
两者走同一条主 loop,区别只在唤醒它的是谁。**由复核 fork 唤醒的 run 是第三类**:
它不计入空转账本(理由见 `docs/adr/0009-*`)。

## 空转

主 agent 被唤醒之后没有真的做事 —— 只说话、跑空操作命令(`echo` / 注释 / `sleep`)、
或者直接去休息。空转的治理**不归潜意识 fork 管**:fork 只负责喊一嗓子,喊了不动是空转
治理腿的事。

## 唤醒 loop

「主 agent 停下来 → 潜意识 fork 唤醒 → 主 agent 又停下来 → 再唤醒」这个自我驱动的循环。
它是小腻在没有外部消息时仍然继续活动的机制。
