import { EventEmitter } from 'events';
import { WebSocketConfig } from '../types';
export declare class WebSocketClient extends EventEmitter {
    private ws;
    private config;
    private moduleLogger;
    private reconnectInterval;
    private maxReconnectAttempts;
    private reconnectAttempts;
    private isConnecting;
    private isManualClose;
    constructor(config: WebSocketConfig);
    connect(): Promise<void>;
    private handleOpen;
    private handleMessage;
    private handleQQMessage;
    private normalizeMessage;
    private extractTextFromMessageSegments;
    private handleQQNotice;
    private handleQQRequest;
    private handleQQMetaEvent;
    private isApiResponse;
    private handleApiResponse;
    private handleError;
    private handleClose;
    private scheduleReconnect;
    isConnected(): boolean;
    sendMessage(data: any): Promise<void>;
    sendPrivateMessage(userId: number, message: string): Promise<void>;
    sendGroupMessage(groupId: number, message: string): Promise<void>;
    sendReplyMessage(messageId: number, message: string): Promise<void>;
    sendAtMessage(groupId: number, userId: number, message: string): Promise<void>;
    deleteMessage(messageId: number): Promise<void>;
    getGroupList(): Promise<any>;
    getGroupMemberList(groupId: number): Promise<any>;
    setFriendAddRequest(flag: string, approve: boolean, remark?: string): Promise<void>;
    setGroupAddRequest(flag: string, subType: string, approve: boolean, reason?: string): Promise<void>;
    close(): void;
    getConnectionInfo(): any;
}
export default WebSocketClient;
//# sourceMappingURL=websocket-client.d.ts.map