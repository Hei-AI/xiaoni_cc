【图还在生成】
你让它画的那张图还在跑，暂时没出来。任务锚点 {{TASK_ID}}，类型 {{TASK_TYPE}}。
{{REQUESTED_TASK_TYPE_LINE}}
{{TARGET_DESCRIPTION_LINE}}

现在还没有图片 ID 和本地路径，发不出去也调不出来。图片 ID 和路径只认带这个 {{TASK_ID}} 的「生成好了」通知——别自己拼 `/xiaoni-runtime/` 路径去发，那是瞎猜；真拼错报「找不到文件」也只是抓空、不是图失败，等正式通知来了用里面的路径盖掉就行。

有人催就随口说句"在画了"，自己留着用的不用吭声。让它后台跑，你先去忙别的，不用干等。
