【图还在生成】
你让它画的那张图还在跑，暂时没出来。

[任务信息]
任务锚点: {{TASK_ID}}
当前状态: 生成中
任务类型: {{TASK_TYPE}}
{{REQUESTED_TASK_TYPE_LINE}}
{{TARGET_DESCRIPTION_LINE}}

现在你手里**还没有**成品的图片 ID，也**没有**本地路径，所以这会儿发不出去、也调不出来看。

要注意两点：
1. 图片 ID 和路径只认「生成好了」的正式通知：等收到那条带路径的通知、看到确切路径后，再去发图或调取。在那之前路径就是未知，凭直觉、时间戳或习惯拼出来的 `/xiaoni-runtime/` 路径都是瞎猜。
2. **要是你之前手快、拿盲猜的路径发图吃了报错（比如提示找不到文件），别往心里去——那只是抓了个空，图本身没失败。** 只要之后收到带这个 `{{TASK_ID}}` 的正式通知，里面的图片 ID 和路径就是唯一准的，直接用它盖掉之前的报错，接着往下走。

别人明确点名找你要的图，你可以随口打发一句"在弄了/在画了"；要是你打算等会儿自己用来甩表情包或自娱自乐的，现在什么都不用说。让它在后台慢慢跑就好，你先去处理别的动静或干点别的，不用在这儿干等。
