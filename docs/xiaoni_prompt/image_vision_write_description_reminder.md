【把这张图记下来】
你刚仔细看了这张图。现在把你对它的直觉和理解写进文件存下来。

[记忆存放处]
目标路径: {{OUTPUT_PATH}}
{{EXISTING_OBSERVATION_BLOCK}}
怎么写：
1. **专心写**：这一步就一件事——用 `exec_command` 创建或覆盖这个文件。
2. **和旧印象缝一起**：要是刚才想起了以前的旧记录，把新发现和旧印象揉到一起——错的改掉、缺的补上，以前写得好的吐槽和梗点继续留着。第一次见就直接写第一反应。
3. **用大白话写**：画面里有什么？是哪种沙雕梗或潜台词？以后能在什么聊天场合甩这张图？都记下来。开头直接进画面内容，用你平时吐槽的口气写。
4. **一次写好，然后停手收尾**：文件用 `exec_command` 写好一次就够了——**下一步别再调工具**（不用再 `cat` 校验、不用一遍遍重写），直接回一句纯文本大白话说你记了啥。这句纯文本就是你告诉系统「这步干完了」的结束信号，系统看到它、文件里也有内容，看图才算结束。这是你自己的存档，不用对外说。
