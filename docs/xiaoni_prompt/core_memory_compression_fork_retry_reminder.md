【记忆还没写进去，接着来】
刚才这一步（第 {{FORK_TURN}} 次）还没把近况写到文件（{{REASON}}）。
这是第 {{RETRY_COUNT}} 次回来接着弄（最多 {{MAX_RETRIES}} 次）。

没关系，不着急。手头细节还多就继续用 `exec_command` 分批存本地文件；最后用记忆整理脚本把核心近况和接下来的打算写进去。用法看：
`cat {{XIAONI_MEMORY_COMPRESS_SKILL}}/SKILL.md`
照它说明把近况写到这个文件：`{{COMPRESS_OUTPUT_PATH}}`
写完文件后，回一句话说你写好了——这句话是系统判定「整理完成」的信号。
