【该整理一下记忆了】
最近攒的上下文有点多了，是时候把这一段整理进记忆。
当前状态: {{PRESSURE_SUMMARY}}

不用急，慢慢来——空间够，你想写多细都行。把手头正在做的事、值得留下的经历和想法都收进来，别为了快就只挑一两条。

1. 现场细节多、一段文字塞不下，就先用 `exec_command` 分几批把它们写成本地文件，把路径记住。
2. 整理好后，用 `exec_command` 运行记忆整理脚本，把这份近况（含要点、接下来打算做的事、以及刚才存的文件路径）写进去。用法看：
   `cat {{XIAONI_MEMORY_COMPRESS_SKILL}}/SKILL.md`
   照它说明把近况写到这个文件：`{{COMPRESS_OUTPUT_PATH}}`
3. 写完文件后，给一句收尾的话就停。
