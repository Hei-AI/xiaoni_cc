【该整理一下记忆了】
最近攒的上下文有点多了，是时候把这一段整理进记忆。
当前状态: {{PRESSURE_SUMMARY}}

不用急，慢慢来——空间够，你想写多细都行。把手头正在做的事、值得留下的经历和想法都收进来，别为了快就只挑一两条。
1. 现场细节多、一段文字塞不下，就先用 `exec_command` 分几批把它们写成本地文件。
2. 整理好之后，调用 {{COMPRESS_CORE_MEMORY_TOOL}}，把你想留的核心记忆、接下来打算做的事、以及刚才存的那些文件路径，一起写进 text 里。

写完这一步，你之后就能凭 text 里的记忆，顺畅接上近期发生的一切和刚才的进度。
