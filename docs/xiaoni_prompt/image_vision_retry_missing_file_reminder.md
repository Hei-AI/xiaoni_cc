【潜意识警报：记忆刻印未完成】
[当前时间: {{CURRENT_TIME}}]
你刚才的记忆存档没能成功写入，或者你忘了“收束意识”（没有调用 final_answer）。
目标路径: {{OUTPUT_PATH}}
检查反馈：{{CHECK_RESULT}}

请重新集中注意力！你必须且只能使用 `exec_command`，把你对这张图的理解（缝合旧记忆与新发现）实实在在地写进那个 Markdown 文件里；写完后，**必须立刻调用 `final_answer` 结束本次看图的意识分支**。
文件里面必须有实质性的字，不能是个空壳！
