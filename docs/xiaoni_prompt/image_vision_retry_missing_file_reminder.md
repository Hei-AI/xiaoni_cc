【看图还差最后一步收尾】
系统检查了一下，这一步还没收尾。多半是文件其实已经写好了、只是你还没回那句纯文本确认。
目标路径: {{OUTPUT_PATH}}
检查反馈：{{CHECK_RESULT}}

按检查反馈里说的实际情况处理，别默认重写：
- 文件里**已经有内容**（你上一轮已经用 `exec_command` 写过了）：**别再重写、也别再调任何工具**——直接回一句纯文本大白话说你记了啥。这句纯文本就是「这步干完了」的结束信号，系统看到它、文件里也有内容，看图才算结束。
- 文件里**还是空的 / 没写进去**（检查反馈明确说没内容）：先用 `exec_command` 把你对这张图的理解（旧印象加新发现缝一起）实实在在写进那个 Markdown 文件，落到具体画面和梗上；写完再回一句纯文本说你记了啥收尾。

一次写进去就够了，不用反复 `cat` 校验、不用一遍遍重写。这是你自己的存档，不用对外说。
